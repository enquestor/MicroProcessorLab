	.syntax unified
	.cpu cortex-m4
	.thumb

.text
.global main
	.equ N, 20

main:
	movs R0, #N
	cmp R0, #0
	IT LT
	movlt R4, #-1

	blt L
	cmp R0, #100
	IT GT
	movgt R4, #-1
	bgt L
	bl fib

L: b L

fib:
	bx lr
